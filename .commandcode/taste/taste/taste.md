# Taste
- Communicates in Bahasa Indonesia and expects replies in the same language. Confidence: 0.8
- Keeps step-by-step task instructions in a notes markdown file at the project root (e.g., `F:\sinotaris\catatan`) and expects the agent to read it and continue the unfinished process described there, rather than re-deriving the plan; feature requests are often phrased tersely as "as written in catatan" with the notes file as the authoritative spec. Confidence: 0.9
- Handles handoffs of interrupted work by pointing the agent at the notes file plus git uncommitted changes as the source of truth for progress; expects the agent to inspect `git status`/`git diff` to reconstruct what is already done before continuing. Confidence: 0.8
- Expects the agent to work end-to-end without being asked to stop for confirmation on every sub-decision (resumes a stalled process autonomously and verifies via live API/E2E tests). Confidence: 0.5
- Wants project/session context persisted so future sessions do not require re-reading the entire project; proactively asks how to save the work session for later continuation. Confidence: 0.6
- Open to adopting Command Code's built-in project memory (AGENTS.md) as a durable project map that auto-loads each session; explicitly approved creating AGENTS.md at the project root containing architecture map, run commands, and uncommitted-work status. Confidence: 0.9
- Sensitive to feature regressions: treats previously-working UI behavior (as in the committed baseline) as something that must survive new work; explicitly called out that upload/replace-per-point, preview-in-new-tab, and green "uploaded" indicators in Kelengkapan Dokumen vanished after an agent handoff, and expects regressions to be diagnosed against the committed version and restored rather than accepted as new behavior. Also states this requirement proactively when requesting a removal/refactor (e.g., "pastikan semua fitur create order berjalan normal seperti sebelum nya"). Confidence: 0.8
- Prefers a lean UI without duplicated functionality: when one section's content is already fully covered by another section, wants the redundant section taken out outright rather than kept (e.g., removed the order-level Berkas Kelengkapan card because document upload already happens inside the Pihak and Aset cards), and expects the removal to be complete — related state, handlers, and references cleaned up, not just hidden. Confidence: 0.6
- Sends a short status check-in shortly after submitting a new task if the agent has not yet acknowledged or visibly started it (e.g., "kamu sedang apa? apa kamu kesulitan?"); expects prompt acknowledgment and immediate progress rather than long silent investigation before the first reply. Confidence: 0.5
- Checks whether the agent still retains an earlier instruction in context (e.g., "kamu bisa baca perintah sebelum ini?") instead of restating the full request; expects a brief confirmation and immediate continuation of the already-issued task rather than re-deriving or re-asking for the spec. Confidence: 0.5
- Wants parity between the multi-section create flow and the detail view: expects the forms, fields, and uploads offered in each create section to be reflected in the detail order page, and when content captured at create time does not appear in detail, asks the agent to investigate the actual data flow (create → persistence → detail render) and align the two screens — not dismiss it as user error or settle for a cosmetic fix. Confidence: 0.5
- Expects conditional/dependent form fields to be hidden, not just shown as irrelevant: spouse-related data (name/NIK pasangan and KTP/NPWP pasangan uploads) must only appear when the controlling value (marital status = menikah) is selected, and switching the control away should clear that data so it is never submitted. Confidence: 0.7
- Expects detail/view screens to be editable in place, not read-only: asks for an "Ubah" button at the end of each section card that opens a modal form prefilled with the same fields used at create time (order info, each party, each asset), with changes saved via the existing update endpoints and the same conditional-field logic applied inside the modals. Confidence: 0.7
- Wants an activity/audit log of data changes as a first-class UI element: a "Log Aktivitas" card listing who changed what and when for an order, which implies backend logging at every mutation (create/update/delete of order, parties, assets, and file uploads) plus a dedicated read endpoint, verified end-to-end. The expectation extends to the process/timeline workflow too: stage started/completed, PIC assignment/transfer, and stage document upload/delete must also write log entries (user explicitly asked whether timeline changes populate the log and expected them to). Confidence: 0.8
- Prefers long detail pages to be broken into collapsible/foldable sections (click the card header to fold/unfold, chevron indicator), while keeping the core workflow section (Timeline Proses) always open and non-collapsible; asked explicitly for "setiap section di-collapse, kecuali section timeline proses". Confidence: 0.7
- Collapsing a section must hide everything except its header: action buttons (e.g., "Ubah"/edit) must not sit in the header's right slot where they stay visible when the card is folded — they belong inside the section body, below the header, so they hide along with the content when collapsed. Confidence: 0.7
- Collapsing a section must hide everything except its header: action buttons (e.g., "Ubah"/edit) must not sit in the header's right slot where they stay visible when folded — they belong inside the section body (below the header) so they hide along with the content when collapsed. Confidence: 0.7
- Treats the same logical document (same catalog key, e.g. IMB/SLF/SPPT) shown in different UI sections of the detail page (card Aset vs the order-level Kelengkapan Dokumen checklist) as one single document: expects uploading it in one place to mark it uploaded/complete everywhere (single source of truth), not two disconnected storage silos; when reconciling this, expects no duplicate records and preview/edit actions routed back to the file's actual origin. Confidence: 0.6
- Consults the agent before committing to structural UI changes: reports a problem and in the same breath asks for a recommendation on layout (e.g., whether to split the long order-detail page into two tabs — Data vs Step/Dokumen Proses), expecting a reasoned yes/no with rationale and a proposed content grouping rather than immediate implementation. Confidence: 0.4
- Prefers cascading/gated option selection in create forms: when choices belong to higher-level categories, wants the category chosen first (e.g., Notaris vs PPAT as a visible pre-step), the dependent dropdown (Jenis Order) disabled with no options until the category is picked, its options filtered to only that category, and the selection reset when the category changes — the type must always depend on the chosen category, never list everything mixed together. Confidence: 0.6
- Wants categorical classification of a record to be visible where it is reviewed, not only captured at creation: e.g., asked that the Notaris/PPAT category of an order be displayed in the detail view (badge/info row), with the agent extending the same badge to list views and filters so the classification is always identifiable at a glance. Confidence: 0.6
